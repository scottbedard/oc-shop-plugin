# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true

tabs:
    fields:
        foo:
            label: foo
            tab: foo

secondaryTabs:
    fields:
        history:
            label: bedard.shop::lang.carts.form.history
            type: Bedard\Shop\FormWidgets\CartHistory
