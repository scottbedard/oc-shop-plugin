# ===================================
#  Form Field Definitions
# ===================================

fields:
    id:
        label: ID
        disabled: true

tabs:
    fields:
        foo:
            label: foo
            tab: foo

secondaryTabs:
    fields:
        bar:
            label: bar
            tab: bar
